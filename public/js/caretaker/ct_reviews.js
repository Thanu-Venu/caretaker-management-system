// For future dynamic loading or sorting
document.addEventListener("DOMContentLoaded", () => {
    const table = document.getElementById("feedbackTable");

    // Example: you can add sorting or filtering functionality here later
    console.log("Feedback page loaded. Table has " + table.rows.length + " rows.");
});
