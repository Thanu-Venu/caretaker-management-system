/**
 * HR report charts are initialized inline in `hr_reports.php` (same pattern as admin reports)
 * so Chart.js load order is guaranteed. This file is kept for backwards compatibility if linked.
 */
